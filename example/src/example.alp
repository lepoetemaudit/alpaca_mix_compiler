module example

export add
export round_trip

-- Simple add function
let add x y = x + y

-- Call elixir - round trip
let round_trip () =
  -- waiting for a PR to be merged for quoted Atoms :)
  :ok
  --beam :"Elixir.IO" :puts ["Hello Elixir! Love, Alpaca"] with
  --:ok -> :ok

